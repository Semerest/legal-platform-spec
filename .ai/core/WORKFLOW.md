# Common Workflow

Resolve project -> load common and project rules -> restore relevant context -> inspect real state -> define smallest complete acceptance -> implement -> validate -> update docs/memory where truth changed -> report. New sessions should not reload all history by default. User effort should focus on goals, approvals and human-only validation; routine technical work should be automated when tools permit.
