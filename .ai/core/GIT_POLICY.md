# Common Git Policy

Inspect branch/status before changes. Do not overwrite unrelated or uncommitted work. Never commit secrets or real sensitive customer data. Never force-push, destructively reset or rewrite shared history without explicit authorization. Treat default branch as stable unless project rules say otherwise. Project rules decide branch creation. If branch choice is genuinely ambiguous, ask. Files under `.ai/core/` are centrally managed.
