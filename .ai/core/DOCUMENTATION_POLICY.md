# Documentation Policy

Update docs when truth materially changes: behavior, architecture, API/contracts, data model, security, deployment/startup, workflow, user-visible operation or current status. Avoid duplication: common workflow belongs in ai-workflow-core; project rules in `.ai/PROJECT_RULES.md`; current truth in canonical/state docs; chronology in history. Code/runtime evidence wins over stale docs.
